# Virtual file neesting in VS Code

add the following config to your vscode settings to declutter side explorer:

```js
{
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.patterns": {
    "config": ".prettierrc.json, .prettierignore, gulpfile.js, package.json, LICENSE, .gitignore"
  }
}
```
